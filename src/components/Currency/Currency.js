import { StyledCurrency } from "./Currency.styles"

export default function Currency(props) {
  // TODO: implement currency selection functionality
  return (<StyledCurrency>{props.currency}</StyledCurrency>)
} 