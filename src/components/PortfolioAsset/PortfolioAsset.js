export default function PortfolioAsset(props) {
  const { coin } = props;

  console.log(coin.coin)

  return (
    <>
      {/*{Object.entries(coin).map(([key, val]) => (
        <p>
          {key}, {val}
        </p>
      ))}*/}
    </>
  );
}
